# GIT指令

## Push


```
$ git add . 

$ git commit -m ""

$ git remote add <name> <URL>
    git remote     //check remote

$ git push -u <name> master

$ git remote add <name> HEAD:<branch name> <URL>    //remote branch

$ git push -u <name> HEAD:<branch name>

```
